{% extends 'GoproVipacReporteBundle::layout.html.twig' %}
{% block content %}
    <h1>Modificar campo</h1>
    {{ form(edit_form) }}
    <ul class="registroAcciones">
        <li>
            <a class="boton" href="{{ path('campo') }}">
                Volver a la lista
            </a>
        </li>
        <li>{{ form(delete_form) }}</li>
    </ul>
{% endblock content %}
