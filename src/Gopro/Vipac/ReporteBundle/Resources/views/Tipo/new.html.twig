{% extends 'GoproVipacReporteBundle::layout.html.twig' %}
{% block content %}
    <h1>Creación de tipos de campo</h1>
    {{ form(form) }}
    <ul class="listaAcciones">
        <li>
            <a href="{{ path('tipo') }}">
                Volver a la lista
            </a>
        </li>
    </ul>
{% endblock content %}
