{% extends 'GoproVipacReporteBundle::layout.html.twig' %}
{% block content %}
    <h1>Lista de sentencias SQL</h1>

    <table class="listaRegistros">
        <thead>
            <tr>
                <th>Id</th>
                <th>Nombre</th>
                <th>Descripción</th>
                <th>Contenido</th>
                <th>Acciones</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('sentencia_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.nombre }}</td>
                <td>{{ entity.descripcion }}</td>
                <td>{{ entity.contenido }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('sentencia_show', { 'id': entity.id }) }}">mostrar</a>
                    </li>
                    <li>
                        <a href="{{ path('sentencia_edit', { 'id': entity.id }) }}">editar</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <ul class="listaAcciones">
        <li>
            <a href="{{ path('sentencia_new') }}">
                Crear un nuevo registro
            </a>
        </li>
    </ul>
{% endblock content %}
