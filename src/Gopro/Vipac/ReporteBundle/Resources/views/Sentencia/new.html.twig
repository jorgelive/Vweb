{% extends 'GoproVipacReporteBundle::layout.html.twig' %}
{% block content %}
    <h1>Creación de sentencias SQL</h1>
    {{ form(form) }}
    <ul class="listaAcciones">
        <li>
            <a class="boton" href="{{ path('sentencia') }}">
                Volver a la lista
            </a>
        </li>
    </ul>
{% endblock content %}
