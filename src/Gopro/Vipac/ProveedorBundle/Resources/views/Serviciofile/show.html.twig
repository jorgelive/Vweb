{% extends 'GoproVipacProveedorBundle::layout.html.twig' %}
{% block title %}Tipo de caracteristica{% endblock title %}
{% block titulo %}Tipo de caracteristica{% endblock titulo %}
{% block content %}
    <table class="registroPropiedades">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
            <tr>
                <th>File</th>
                <td>{{ entity.nombre }}</td>
            </tr>
            <tr>
                <th>Número de pasajeros</th>
                <td>{{ entity.numpax }}</td>
            </tr>
        </tbody>
    </table>

    <ul class="registroAcciones">
        <li>
            <a class="boton" href="{{ path('gopro_vipac_proveedor_serviciofile') }}">
                Volver a la lista
            </a>
        </li>
        <li>
            <a class="boton" href="{{ path('gopro_vipac_proveedor_serviciofile_edit', { 'id': entity.id }) }}">
                Editar
            </a>
        </li>
        <li>
            <a class="boton" href="{{ path('gopro_vipac_proveedor_serviciofile_new') }}">
                Crear otro
            </a>
        </li>
        <li>{{ form(delete_form) }}</li>
    </ul>
{% endblock content %}
